// Herda propriedades do pai (obj_parente_inimigo)
event_inherited();

// Propriedades do golpe da machadada
vida = 999;
hit = false;
item_drop = noone;

// Torna o objeto invisível na tela (funciona apenas como hitbox física)
visible = false;
